apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-user-api-deploy
  namespace: default
spec:
  replicas: 2
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: demo-user-api-deploy
  template:
    metadata:
      labels:
        app: demo-user-api-deploy
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: demo-user-api
        image: dnjsakf/demo-user-api:latest
        ports:
        - containerPort: 9092
