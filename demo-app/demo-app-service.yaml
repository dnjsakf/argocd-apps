apiVersion: v1
kind: Service
metadata:
  name: demo-app
  labels:
    app: demo-app
spec:
  type: NodePort
  selector:
    name: demo-app
  sessionAffinity: None
  ports:
  - name: http
    nodePort: 38080
    port: 80
    protocol: TCP
    targetPort: 8080
