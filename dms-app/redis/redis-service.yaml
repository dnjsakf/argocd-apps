apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: default
spec:
  type: ExternalName
  externalName: dev.dms-app.com
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
