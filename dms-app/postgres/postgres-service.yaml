apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: default
spec:
  type: ExternalName
  externalName: 192.168.0.61
  # type: NodePort
  # ports:
  #   - protocol: TCP
  #     port: 5432
  #     targetPort: 5432
  #     nodePort: 30002
